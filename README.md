# orientationJ_mean_polar_plot
This Python script analyzes coherency-weighted orientation histograms from OrientationJ. For each embryo, mirror angles (−θ and +θ) are combined, rebinned, and normalized by the sum of histogram values per frame. Distributions are averaged across embryos and renormalized to sum to 1 before polar plotting.
